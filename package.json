{
  "name": "spock",
  "version": "0.1.0",
  "scripts": {
    "dev": "cd web && bun run vite dev",
    "check": "cd web && bun run svelte-kit sync && bun run svelte-check --tsconfig ./tsconfig.json",
    "check:watch": "cd web && bun run svelte-kit sync && bun run svelte-check --tsconfig ./tsconfig.json --watch",
    "server": "cd server && bun dev",
    "db": "cd db && ./pocketbase serve"
  },
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
