{
  "name": "spock",
  "version": "0.1.1",
  "scripts": {
    "dev": "cd web && bun dev",
    "check": "cd web && bun check",
    "check:watch": "cd web && bun check:watch",
    "server": "cd server && bun dev",
    "db": "cd db && ./pocketbase serve",
    "format": "prettier --write ."
  },
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "3.3.3"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
